image: golang:latest
pipelines:
  default:
    - step:
        script:
          - go version
          - go vet ./...
          - go test -race ./...
          - go test -cover ./...
          - go test -bench . -benchmem ./...
