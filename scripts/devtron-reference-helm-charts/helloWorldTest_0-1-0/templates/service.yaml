apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloWorldTest.fullname" . }}
  labels:
    {{- include "helloWorldTest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helloWorldTest.selectorLabels" . | nindent 4 }}
