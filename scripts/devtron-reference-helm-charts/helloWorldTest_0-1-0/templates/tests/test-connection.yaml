apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helloWorldTest.fullname" . }}-test-connection"
  labels:
    {{- include "helloWorldTest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helloWorldTest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
