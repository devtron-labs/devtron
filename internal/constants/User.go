package constants

const (
	AdminUser  string = "admin"
	SystemUser string = "system"
)

const (
	AdminUserId  = 1 // we have established Admin user as 1 while setting up devtron
	SystemUserId = 2 // we have established System user as 1 while setting up devtron, which are being used for auto-trigger operations
)
