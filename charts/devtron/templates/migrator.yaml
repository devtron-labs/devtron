{{- with .Values.components.migrator }}
---
{{- if $.Capabilities.APIVersions.Has "batch/v1/Job" }}
apiVersion: batch/v1
{{- else }}
apiVersion: batch/v1beta1
{{- end }}
kind: Job
metadata:
  name: postgresql-migrate-devtron-{{ randAlphaNum 5 | lower }}
  annotations: {}
  labels:
    job: postgresql-migrate-devtron
spec:
  template:
    spec:
      {{- if and $.Values.global $.Values.global.podSecurityContext }}
      securityContext:
{{- toYaml $.Values.global.podSecurityContext | nindent 8 }}
      {{- end }}
      initContainers:
        - command:
            - /bin/sh
            - -c
            - cp -r /scripts/. /shared/
          {{- if $.Values.installer.modules }}
          {{- if and (has "cicd" $.Values.installer.modules) ($.Release.IsUpgrade) }}
          image: {{ $.Values.components.devtron.cicdImage }}
          {{- else }}
          image: {{ $.Values.components.devtron.image }}
          {{- end }}
          {{- else }}
          image: {{ $.Values.components.devtron.image }}
          {{- end }}
          name: init-devtron
          volumeMounts:
            - mountPath: /shared
              name: shared-volume
      containers:
        - command:
            - /bin/sh
            - -c
            - 'if [ $(MIGRATE_TO_VERSION) -eq "0" ]; then migrate -path $(SCRIPT_LOCATION) -database postgres://$(DB_USER_NAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)?sslmode=disable up;  else   echo $(MIGRATE_TO_VERSION); migrate -path $(SCRIPT_LOCATION)  -database postgres://$(DB_USER_NAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)?sslmode=disable goto $(MIGRATE_TO_VERSION);    fi '
          env:
            {{- if $.Values.components.migrator.envVars.devtron.SCRIPT_LOCATION }}
            - name: SCRIPT_LOCATION
              value: {{ $.Values.components.migrator.envVars.devtron.SCRIPT_LOCATION }}
            {{- else }}
            - name: SCRIPT_LOCATION
              value: /shared/sql/
            {{- end }}
            - name: DB_TYPE
              value: postgres
            - name: DB_USER_NAME
              value: postgres
            - name: DB_HOST
              value: postgresql-postgresql.devtroncd
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: {{ $.Values.components.migrator.envVars.devtron.DB_NAME }}
            {{- if $.Values.components.migrator.envVars.devtron.MIGRATE_TO_VERSION }}
            - name: MIGRATE_TO_VERSION
              value: {{ $.Values.components.migrator.envVars.devtron.MIGRATE_TO_VERSION }}
            {{- else }}
            - name: MIGRATE_TO_VERSION
              value: "0"
            {{- end }}
          envFrom:
            - secretRef:
                name: postgresql-migrator
          {{- if $.Values.components.migrator.image }}        
          image: {{ $.Values.components.migrator.image }}    
          {{- else }}
          image: migrate/migrate
          {{- end }} 
          name: postgresql-migrate-devtron
          volumeMounts:
            - mountPath: /shared
              name: shared-volume
          {{- if .devtron }}
          {{- if .devtron.resources }}        
          resources:
          {{- toYaml .devtron.resources | nindent 10 }}
          {{- end }}
          {{- end }}           
      restartPolicy: OnFailure
      volumes:
        - emptyDir: {}
          name: shared-volume 
  backoffLimit: 20
  activeDeadlineSeconds: 1500
---
{{- if $.Capabilities.APIVersions.Has "batch/v1/Job" }}
apiVersion: batch/v1
{{- else }}
apiVersion: batch/v1beta1
{{- end }}
kind: Job
metadata:
  name: postgresql-migrate-casbin-{{ randAlphaNum 5 | lower }}
  annotations: {}
spec:
  template:
    spec:
      serviceAccountName: devtron
      {{- if and $.Values.global $.Values.global.podSecurityContext }}
      securityContext:
{{- toYaml $.Values.global.podSecurityContext | nindent 8 }}
      {{- end }}
      containers:
      - name: devtron-rollout
        image: "quay.io/devtron/kubectl:latest"
        command: ['sh', '-c', 'kubectl rollout restart deployment/devtron -n devtroncd && kubectl rollout restart deployment/kubelink -n devtroncd']
        {{- if .casbin }}
        {{- if .casbin.resources }}        
        resources:
        {{- toYaml .casbin.resources | nindent 10 }}
        {{- end }}
        {{- end }} 
      initContainers:
        - command:
            - /bin/sh
            - -c
            - cp -r /scripts/. /shared/
          {{- if $.Values.installer.modules }}
          {{- if and (has "cicd" $.Values.installer.modules) ($.Release.IsUpgrade) }}
          image: {{ $.Values.components.devtron.cicdImage }}
          {{- else }}
          image: {{ $.Values.components.devtron.image }}
          {{- end }}
          {{- else }}
          image: {{ $.Values.components.devtron.image }}
          {{- end }}  
          name: init-devtron
          volumeMounts:
            - mountPath: /shared
              name: shared-volume
        - command:
            - /bin/sh
            - -c
            - 'if [ $(MIGRATE_TO_VERSION) -eq "0" ]; then migrate -path $(SCRIPT_LOCATION) -database postgres://$(DB_USER_NAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)?sslmode=disable up;  else   echo $(MIGRATE_TO_VERSION); migrate -path $(SCRIPT_LOCATION)  -database postgres://$(DB_USER_NAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)?sslmode=disable goto $(MIGRATE_TO_VERSION);    fi '
          env:
            {{- if $.Values.components.migrator.envVars.casbin.SCRIPT_LOCATION }}
            - name: SCRIPT_LOCATION
              value: {{ $.Values.components.migrator.envVars.casbin.SCRIPT_LOCATION }}
            {{- else }}
            - name: SCRIPT_LOCATION
              value: /shared/casbin/
            {{- end }}
            - name: DB_TYPE
              value: postgres
            - name: DB_USER_NAME
              value: postgres
            - name: DB_HOST
              value: postgresql-postgresql.devtroncd
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: {{ $.Values.components.migrator.envVars.casbin.DB_NAME }}
            {{- if $.Values.components.migrator.envVars.casbin.MIGRATE_TO_VERSION }}
            - name: MIGRATE_TO_VERSION
              value: {{ $.Values.components.migrator.envVars.casbin.MIGRATE_TO_VERSION }}
            {{- else }}
            - name: MIGRATE_TO_VERSION
              value: "0"
            {{- end }}
          envFrom:
            - secretRef:
                name: postgresql-migrator
          {{- if $.Values.components.migrator.image }}        
          image: {{ $.Values.components.migrator.image }}    
          {{- else }}
          image: migrate/migrate
          {{- end }} 
          name: postgresql-migrate-casbin
          volumeMounts:
            - mountPath: /shared
              name: shared-volume  
      restartPolicy: OnFailure
      volumes:
        - emptyDir: {}
          name: shared-volume 
  backoffLimit: 20
  activeDeadlineSeconds: 1500

  
{{- if $.Values.installer.modules }}
{{- if has "cicd" $.Values.installer.modules }}
---
{{- if $.Capabilities.APIVersions.Has "batch/v1/Job" }}
apiVersion: batch/v1
{{- else }}
apiVersion: batch/v1beta1
{{- end }}
kind: Job
metadata:
  name: postgresql-migrate-gitsensor-{{ randAlphaNum 5 | lower }}
spec:
  template:
    spec:
      {{- if and $.Values.global $.Values.global.podSecurityContext }}
      securityContext:
{{- toYaml $.Values.global.podSecurityContext | nindent 8 }}
      {{- end }}
      initContainers:
        - command:
            - /bin/sh
            - -c
            - cp -r sql /shared/
          {{- if $.Values.components.gitsensor.image }}
          image: {{ $.Values.components.gitsensor.image }}
          {{- end }}
          name: init-git-sensor
          volumeMounts:
            - mountPath: /shared
              name: shared-volume
      containers:
      - command:
            - /bin/sh
            - -c
            - 'if [ $(MIGRATE_TO_VERSION) -eq "0" ]; then migrate -path $(SCRIPT_LOCATION) -database postgres://$(DB_USER_NAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)?sslmode=disable up;  else   echo $(MIGRATE_TO_VERSION); migrate -path $(SCRIPT_LOCATION)  -database postgres://$(DB_USER_NAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)?sslmode=disable goto $(MIGRATE_TO_VERSION);    fi '
          env:
            {{- if $.Values.components.migrator.envVars.gitsensor.SCRIPT_LOCATION }}
            - name: SCRIPT_LOCATION
              value: {{ $.Values.components.migrator.envVars.gitsensor.SCRIPT_LOCATION }}
            {{- else }}
            - name: SCRIPT_LOCATION
              value: /shared/sql/
            {{- end }}
            - name: DB_TYPE
              value: postgres
            - name: DB_USER_NAME
              value: postgres
            - name: DB_HOST
              value: postgresql-postgresql.devtroncd
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: {{ $.Values.components.migrator.envVars.gitsensor.DB_NAME }}
            {{- if $.Values.components.migrator.envVars.gitsensor.MIGRATE_TO_VERSION }}
            - name: MIGRATE_TO_VERSION
              value: {{ $.Values.components.migrator.envVars.gitsensor.MIGRATE_TO_VERSION }}
            {{- else }}
            - name: MIGRATE_TO_VERSION
              value: "0"
            {{- end }}
          envFrom:
            - secretRef:
                name: postgresql-migrator
          {{- if $.Values.components.migrator.image }}        
          image: {{ $.Values.components.migrator.image }}    
          {{- else }}
          image: migrate/migrate
          {{- end }} 
          name: postgresql-migrate-git-sensor
          volumeMounts:
            - mountPath: /shared
              name: shared-volume
          {{- if .gitsensor }}
          {{- if .gitsensor.resources }}        
          resources:
          {{- toYaml .gitsensor.resources | nindent 10 }}
          {{- end }}
          {{- end }}
      restartPolicy: OnFailure
      volumes:
        - emptyDir: {}
          name: shared-volume 
  backoffLimit: 20
  activeDeadlineSeconds: 1500
---
{{- if $.Capabilities.APIVersions.Has "batch/v1/Job" }}
apiVersion: batch/v1
{{- else }}
apiVersion: batch/v1beta1
{{- end }}
kind: Job
metadata:
  name: postgresql-migrate-lens-{{ randAlphaNum 5 | lower }}
spec:
  template:
    spec:
      {{- if and $.Values.global $.Values.global.podSecurityContext }}
      securityContext:
{{- toYaml $.Values.global.podSecurityContext | nindent 8 }}
      {{- end }}
      initContainers:
        - command:
            - /bin/sh
            - -c
            - cp -r sql /shared/
          {{- if $.Values.lens.image }}
          image: {{ $.Values.lens.image }}
          {{- end }}
          name: init-lens
          volumeMounts:
            - mountPath: /shared
              name: shared-volume
      containers:
      - name: postgresql-migrate-lens
        command:
            - /bin/sh
            - -c
            - 'if [ $(MIGRATE_TO_VERSION) -eq "0" ]; then migrate -path $(SCRIPT_LOCATION) -database postgres://$(DB_USER_NAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)?sslmode=disable up;  else   echo $(MIGRATE_TO_VERSION); migrate -path $(SCRIPT_LOCATION)  -database postgres://$(DB_USER_NAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)?sslmode=disable goto $(MIGRATE_TO_VERSION);    fi ' 
        env:
            {{- if $.Values.components.migrator.envVars.lens.SCRIPT_LOCATION }}
            - name: SCRIPT_LOCATION
              value: {{ $.Values.components.migrator.envVars.lens.SCRIPT_LOCATION }}
            {{- else }}
            - name: SCRIPT_LOCATION
              value: /shared/sql/
            {{- end }}
            - name: DB_TYPE
              value: postgres
            - name: DB_USER_NAME
              value: postgres
            - name: DB_HOST
              value: postgresql-postgresql.devtroncd
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: {{ $.Values.components.migrator.envVars.lens.DB_NAME }}
            {{- if $.Values.components.migrator.envVars.lens.MIGRATE_TO_VERSION }}
            - name: MIGRATE_TO_VERSION
              value: {{ $.Values.components.migrator.envVars.lens.MIGRATE_TO_VERSION }}
            {{- else }}
            - name: MIGRATE_TO_VERSION
              value: "0"
            {{- end }}
          envFrom:
          - secretRef:
              name: postgresql-migrator
          {{- if $.Values.components.migrator.image }}        
          image: {{ $.Values.components.migrator.image }}    
          {{- else }}
          image: migrate/migrate
          {{- end }} 
          {{- if .lens }}
          {{- if .lens.resources }}        
          resources:
          {{- toYaml .lens.resources | nindent 10 }}
          {{- end }} 
          {{- end }}
          volumeMounts:
              - mountPath: /shared
                name: shared-volume
      restartPolicy: OnFailure
      volumes:
        - emptyDir: {}
          name: shared-volume  
  backoffLimit: 20
  activeDeadlineSeconds: 1500
{{- end }}
{{- end }}
{{- end }}
