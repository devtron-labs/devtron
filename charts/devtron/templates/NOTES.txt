
1. Run the following command to get the default admin password. Default username is admin

   kubectl -n devtroncd get secret devtron-secret -o jsonpath='{.data.ACD_PASSWORD}' | base64 -d
{{- if $.Values.installer.modules }}

2. You can watch the progress of Devtron microservices installation by the following command

   kubectl -n devtroncd get installers installer-devtron -o jsonpath='{.status.sync.status}'
{{- end }}

3. You can access devtron dashboard with following.

{{- if and (eq $.Values.components.devtron.service.type "LoadBalancer") ($.Values.components.devtron.ingress.enabled) }} 

   {{ printf $.Values.components.devtron.ingress.host }}
{{- end }}
{{- if and (eq $.Values.components.devtron.service.type "NodePort") ($.Values.components.devtron.ingress.enabled) }} 

   {{ printf $.Values.components.devtron.ingress.host }}
{{- end }}


{{- if and (eq $.Values.components.devtron.service.type "LoadBalancer") (not $.Values.components.devtron.ingress.enabled ) }}

   kubectl get svc -n devtroncd devtron-service -o jsonpath='{.status.loadBalancer.ingress}'
{{- end }}
{{- if and (eq $.Values.components.devtron.service.type "NodePort") ((not $.Values.components.devtron.ingress.enabled ))}}

   Your devtron dashboard is available on http://PUBLIC_IP:{{ .Values.components.devtron.service.nodePort }}

   kubectl get svc -n devtroncd devtron-service
{{- end }}